{
  // :not([mini]) because it sets the file's grammar, and a mini editor has no
  // file of its own. Shift is explicit and the primary key stays lowercase.
  "lumine-text-editor:not([mini])": {
    "ctrl-shift-l": "grammar-selector:show"
  }
}
